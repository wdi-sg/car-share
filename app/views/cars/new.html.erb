<%= form_with scope: :car, url: cars_path, local: true do |form| %>

    <p>
      <%= form.label :name %><br>
      <%= form.text_field :name %>
    </p>
  
    <p>
      <%= form.label :description %><br>
      <%= form.text_area :description %>
    </p>
  
  
      <p>
  
        <%= form.collection_check_boxes :space_ids, @spaces, :id, :name %>
      </p>
  
  
    <p>
      <%= form.submit %>
    </p>
  <% end %>